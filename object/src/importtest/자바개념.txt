<자바 프로젝트 구조>

패키지 = 클래스 묶음(=폴더)
    package 패키지명; (-형태)

import: 다른 패키지의 클래스 사용시 필요
    import 패키지명.클래스명; (-형태)


순서
소스파일 작성 -> 컴파일 (자동으로 이루어짐 bin 폴더에이씀) -> 실행
A.java        -> A.class                                   -> 클래스 실행